export default () => {
  return false;
  // var isTab = undefined
  // if (Platform.OS === 'ios') {
  //   const iPad = 'iPad'

  //   const deviceName = deviceInfoModule.getDeviceNameSync()
  //   const deviceId = deviceInfoModule.getDeviceId()

  //   isTab = deviceId.includes(iPad) || deviceName.includes(iPad) || deviceInfoModule.isTablet()
  // } else {
  //   isTab = deviceInfoModule.isTablet();
  // }
  // // return false
  // return isTab
};
